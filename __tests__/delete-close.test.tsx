// import { render, screen, fireEvent } from '@testing-library/react'
// import DeleteClose from '../src/components/delete-close'

describe('DeleteClose component', () => {
  // it('shows TrashSVG when isEdit is false and triggers onDelete on click', () => {
  //   const onDelete = vi.fn()
  //   const onClose = vi.fn()
  //   render(<DeleteClose isEdit={false} onDelete={onDelete} onClose={onClose} />)
  //   const trashIcon = screen.getByLabelText('Delete todo item')
  //   expect(trashIcon).toBeInTheDocument()
  //   fireEvent.click(trashIcon)
  //   expect(onDelete).toHaveBeenCalled()
  // })
  // it('shows CloseSVG when isEdit is true and triggers onClose on click', () => {
  //   const onDelete = vi.fn()
  //   const onClose = vi.fn()
  //   render(<DeleteClose isEdit={true} onDelete={onDelete} onClose={onClose} />)
  //   const closeIcon = screen.getByLabelText('Exit todo item')
  //   expect(closeIcon).toBeInTheDocument()
  //   fireEvent.click(closeIcon)
  //   expect(onClose).toHaveBeenCalled()
  // })
})
